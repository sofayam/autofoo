#ACHTUNG WICHTIG

# start cntlm

# set proxy (Achtung don't use localhost, use IP Address!)

git config --global https.proxy http://127.0.0.1:3128
git config --global http.proxy http://127.0.0.1:3128


# remember to unset later

# git config --global --edit
